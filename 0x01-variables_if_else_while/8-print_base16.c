#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int z;

for (z = 48; z <= 57; z++)
{
putchar(z);
}
for (z = 97; z <= 102;z++)
{
putchar(z)
}
putchar('\n');
return (0);
}
