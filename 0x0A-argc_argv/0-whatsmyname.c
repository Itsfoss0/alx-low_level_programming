#include "main.h"
/**
 * main - print the name of argv[0]
 * @argv: pointer to the string args
 * @argc: argument count
 * Return:0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
