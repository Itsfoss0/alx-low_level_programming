#include "main.h"
/**
 *mul - multiply two numbers ( a and b)
 *@a: first  number to be multiplied
 *@b: number to be multiplied agains
 *Return: the product of 'a' and 'b'
 */

int mul(int a, int b)
{
return (a * b);
}
